<div class="movie-card col-3 text-wrap text-center mb-3">
  <img src="<%= movie.poster_url %>" alt="<%= movie.title %>">
  <div class="movie-card__content">
    <small class="movie-card__title"><%= movie.title %> (<%= movie.year %>)</small>
  </div>
</div>
