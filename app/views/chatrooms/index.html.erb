<h1>Chatrooms</h1>

<% if @chatrooms.any? %>
  <ul>
    <% @chatrooms.each do |chatroom| %>
      <li>
        <%= link_to chatroom.name, chatroom_path(chatroom) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No messages yet</p>
<% end %>
