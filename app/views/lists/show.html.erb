<h1><%= @list.name %></h1>
<% if @list.movies.empty? %>
  <p>This list is empty.</p>
<% else %>
  <ul>
    <% @list.items.each do |item| %>
      <li><%= item.name %></li>
    <% end %>
  </ul>
<% end %>
